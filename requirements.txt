# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Pydantic
pydantic==2.10.6
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1

# Queue / worker
celery[redis]==5.4.0
redis==5.2.1

# Agent framework
langgraph==0.3.5
langchain==0.3.14
langchain-openai==0.3.0

# LLM (unified interface – supports OpenRouter, NVIDIA NIM, Gemini, Claude, etc.)
litellm==1.63.2

# HTTP
httpx==0.28.1

# Document parsing
pdfplumber==0.11.4        # PDF text extraction (read_document tool)
beautifulsoup4==4.12.3    # HTML content extraction (document_reader + summarise_url)
lxml==5.3.1               # Fast HTML parser for BeautifulSoup

# Utils
python-dotenv==1.1.0
structlog==25.1.0
tenacity==9.0.0
python-jose[cryptography]==3.3.0

# Telegram bot
python-telegram-bot==21.10
